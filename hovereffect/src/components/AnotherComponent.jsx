import HoverComponent from './HoverComponent';

const AnotherComponent = () => {
    return (
        <HoverComponent 
            hoverText="This is some unique hover text for another component."
            normalText="Here is some different content for this component."
        />
    );
};

export default AnotherComponent;
